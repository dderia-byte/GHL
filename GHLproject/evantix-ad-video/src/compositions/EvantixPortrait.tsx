import React from "react";
import { EvantixFilm } from "./EvantixFilm";

/** LinkedIn portrait cut — 1080×1350, 4:5. Renders the shared film timeline. */
export const EvantixPortrait: React.FC = () => <EvantixFilm />;
